// src/components/community/FollowButton.jsx
// Follow has been removed from Ascivan (proof over pedigree — no social follower counts).
// This component is intentionally neutralized: it renders nothing, so every place that
// still imports/uses <FollowButton /> simply shows no button. The underlying followSystem
// utility is left dormant and unreferenced. Safe to delete fully in a later cleanup.

const FollowButton = () => null;

export default FollowButton;
